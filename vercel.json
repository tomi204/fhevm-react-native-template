{
  "installCommand": "corepack enable pnpm && corepack prepare pnpm@10.9.0 --activate && pnpm install --ignore-scripts && pnpm sdk:build",
  "buildCommand": "pnpm --filter ./packages/nextjs build",
  "outputDirectory": "packages/nextjs/.next",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}
